<header class="page-header">
  <p class="eyebrow"><%= page.subtitle || __('menu.about') %></p>
  <h1><%= page.title %></h1>
  <% if (page.description) { %><p><%= page.description %></p><% } %>
</header>
<article class="prose"><%- page.content %></article>
